const superHeroes: string[] = [];
//const superPowers: number[] = [];
const superPowers: Array<number> = [];


type User = {
    name: string;
    isActive: boolean;
}

const allUsers: User[] = [];

const MLModels: number[][] = [
    [255, 255, 255],
    [1, 111, 11]
]

superHeroes.push("spiderman");
superPowers.push(2);
allUsers.push({name: "vadim", isActive: false})